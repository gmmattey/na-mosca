Numero de pontos que conta rapido ate o valor final quando a revelacao termina.

```jsx
<ScoreCounter value={900} />
<ScoreCounter value={1000} size="lg" />
```

Animacao de ~500ms, sem bounce exagerado.